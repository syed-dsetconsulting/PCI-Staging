apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: pci-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi  # Azure Managed Disk
  resources:
    requests:
      storage: 20Gi
